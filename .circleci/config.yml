version: 2
jobs:
  build:
    docker:
      - image: php 
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: PHPLint check
          command: php *.php
    deployment:
      hub:
        commands:
          - docker login -u itamar -p $DOCKER_PASS
          - docker push itamar/hello-dev
