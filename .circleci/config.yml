version: 2
jobs:
  build:
    docker:
      - image: php 
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: PHPLint check
          command: php *.php
  deployment:
    automerge:
      branch: [dev]
      commands:
        - ./merge_to_preprod.sh
